services:
  - type: web
    name: krishverse-portfolio
    runtime: static
    buildCommand: npm run build
    publishPath: build
    routes:
      - type: rewrite
        source: /*
        destination: /index.html
